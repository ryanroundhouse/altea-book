# Mailgun Configuration for Email Notifications
# Get these from your Mailgun dashboard: https://app.mailgun.com/
#
# Note: User credentials (Altea login, notification emails) are now stored
# in users.yaml - see users.example.yaml for the template.

# Your Mailgun domain (e.g., mg.yourdomain.com or sandbox123.mailgun.org)
MAILGUN_DOMAIN=your-mailgun-domain.mailgun.org

# Your Mailgun API key (starts with 'key-')
MAILGUN_API_KEY=key-your-api-key-here

# Email address to send from (must be authorized in Mailgun)
FROM_EMAIL=altea-booking@your-domain.com
